/* ============================================
   THE ALCHEMIST — Transformation Pairs
   Side-by-side before/after (no drag needed)
   ============================================ */

export function initTransformationSlider() {
  // Layout is now CSS-only — no JS interaction required.
}
